<h1>タスク一覧</h1>
<ul>
    <% @tasks.each do |task|%>
    <li>
       <%= task.status %> &gt; <%= task.content %>
        <%= link_to '詳細',task %>
    </li>
    <% end %>
</ul>
<%= link_to 'タスク作成', new_task_path %>